# Установка

Задать настройки в .env файле

Выполнить
```
cd laravel
composer install
php artisan key:generate
php artisan migrate
php artisan db:seed
php artisan elfinder:publish
npm install
npm run production
```